services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: credit_db
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  credit_service:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/credit_db
    depends_on:
      - db
    ports:
      - "8000:8000"

volumes:
  db_data:
